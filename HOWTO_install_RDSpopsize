HOWTO Install RDSpopsize from GitHub or CDC GitLab
--------------------------------------------------

1.  Start an R session.
2.  Install package devtools, if not already installed:

    install.packages("devtools")

3.  Attach devtools:

    library(devtools)

4a. Install RDSpopsize from GitHub (authoratative source):

    devtools::install_github("sgutreuter/RDSpopsize")

    -or-

4b. Install RDSpopsize from CDC GitLab (CDC only):

    url <- "https://git.biotech.cdc.gov/wqh4/RDSpopsize.git"
	 devtools::install_git(url = url)

5.  Attach RDSpopsize and proceed:

    library(RDSpopsize)
	 ?RDSpopsize
